NEXT_PUBLIC_API_URL=http://localhost:8000
# Server-only API origin for login/tracker actions. Never expose credentials here.
API_BASE_URL=http://localhost:8000
# Set the same secret in Vercel and GitHub Actions for the judging-window ping.
CRON_SECRET=replace-with-at-least-16-random-characters
